/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: false,
  i18n: {
    locales: ["en", "fr", "es", "de", "it", "ar"],
    defaultLocale: "en",
  },
}

export default nextConfig
